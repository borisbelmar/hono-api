meta {
  name: Get Notes
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/notes
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

headers {
  Accept: application/json
}

docs {
  Lista las notas del usuario autenticado con su categoría y etiquetas incluidas.

  ## Auth
  Requiere token JWT en el header Authorization.
}
