# Dependencias
node_modules/

# Build
dist/

# Prisma — código generado (se regenera con yarn prisma:generate)
src/generated/

# Variables de entorno — NUNCA subir al repo
.env
.env.*
!.env.example

# Nota: las migraciones de Prisma se commitean (necesarias para prisma migrate deploy en producción)
# prisma/migrations/

# Yarn
.yarn/cache
.yarn/install-state.gz

# Logs
*.log

# Uploads (desarrollo local)
uploads/
